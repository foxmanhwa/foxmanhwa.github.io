# Environment variables — never commit credentials
.env
.env.*
!.env.example

# Admin panel — contains service_role key, local-only, never ships to GitHub Pages
admin.html

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.swp
